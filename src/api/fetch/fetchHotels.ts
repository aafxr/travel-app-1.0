import {Hotel} from "../../core/classes";

export async function fetchHotels(text: string): Promise<Hotel[]>{
    // const response = (await aFetch.post('/hotels/getList/', {text})).data
    // if (response.ok && Array.isArray(response.data))return response.data
    return []
}